package net.minecraftforge.entity;

/**
 * Compile-only stub. At runtime, the real class is provided by:
 *   - Forge platform: net.minecraftforge.entity.IEntityAdditionalSpawnData from the Forge loader
 *   - Fabric platform: not referenced (Create Fabric's AbstractContraptionEntity does not use this interface)
 * This stub is excluded from the final jar (lives in the 'stubs' source set).
 */
public interface IEntityAdditionalSpawnData {
}
